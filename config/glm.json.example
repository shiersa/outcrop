{
  "_comment": [
    "智谱额度接口配置。国内站(open.bigmodel.cn)和海外站(api.z.ai)是两套域名，",
    "端点路径、鉴权方式、响应字段都可能不同，所以不硬编码。",
    "endpoints 是「域名片段 -> 站点配置」的映射：渲染时按 ANTHROPIC_BASE_URL 包含哪个",
    "key 来选站点（多个命中取最长 key），每个站点各自的 url/auth_scheme/fields 独立。",
    "额度缓存也按站点分文件（cache/glm_quota-<站点>.json），切 profile 不会看到别的账号。",
    "跑一次探测拿到真实情况后填这里，改完不用重新编译：",
    "  ~/.config/claude-statusline/claude-statusline --probe-glm",
    "auth_scheme: raw = Authorization: <token>；bearer = Authorization: Bearer <token>",
    "",
    "fields 里每项是候选名列表，dig 递归取第一个命中（大小写不敏感）。候选名支持",
    "「按兄弟字段过滤」：name@k=v&k2=v2，例 percentage@type=TOKENS_LIMIT&unit=3。",
    "",
    "实测（2026-08-21，国内站与 z.ai 均已验证，两站响应结构完全一致）：",
    "  响应是 data.limits[] 数组，三个限额**都叫 percentage**，只能靠 type/unit 区分：",
    "    TIME_LIMIT   unit=5  月度工具调用（search-prime / web-reader / zread）",
    "    TOKENS_LIMIT unit=3  ≈5h 滚动窗   <- 渲染成 '5h' 的就该是这个",
    "    TOKENS_LIMIT unit=6  更长的窗（周/月）",
    "  ⚠️ 曾经按裸名字 percentage 取，dig 命中的是数组第一个 = TIME_LIMIT unit5，",
    "     也就是 5h 那个数字一直在报错的窗口。一次真实响应里前者 1%、后者 13%。",
    "     现在改成带兄弟过滤的选择器，不依赖数组顺序。",
    "  ⚠️ 刻意不留裸 percentage 兜底：过滤没命中说明上游改了 type/unit，这时候退回",
    "     「取第一个」等于恢复那个静默取错的行为 —— 显示不出来比显示错的强。",
    "  z.ai 的端点与国内站同路径，raw / bearer 两种鉴权都返回 200。",
    "",
    "兼容：旧版顶层单 endpoint/auth_scheme/fields 写法仍可用，首次加载自动迁移成",
    "endpoints 结构（原文件备份成 glm.json.bak-<时间戳>，_comment 等顶层键保留）。"
  ],
  "endpoints": {
    "bigmodel.cn": {
      "url": "https://open.bigmodel.cn/api/monitor/usage/quota/limit",
      "auth_scheme": "raw",
      "fields": {
        "used_pct": [
          "percentage@type=TOKENS_LIMIT&unit=3"
        ],
        "mcp_pct": [
          "percentage@type=TIME_LIMIT&unit=5"
        ],
        "used": [
          "used",
          "used_tokens",
          "usedTokens"
        ],
        "total": [
          "total",
          "limit",
          "quota",
          "total_tokens"
        ]
      }
    },
    "z.ai": {
      "url": "https://api.z.ai/api/monitor/usage/quota/limit",
      "auth_scheme": "raw",
      "fields": {
        "used_pct": [
          "percentage@type=TOKENS_LIMIT&unit=3"
        ],
        "mcp_pct": [
          "percentage@type=TIME_LIMIT&unit=5"
        ],
        "used": [
          "used",
          "used_tokens",
          "usedTokens"
        ],
        "total": [
          "total",
          "limit",
          "quota",
          "total_tokens"
        ]
      }
    }
  }
}
