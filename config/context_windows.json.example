{
  "_comment": [
    "模型上下文窗口大小，单位 token。",
    "查找顺序：models 里精确匹配 -> 子串匹配 -> [1m]/1m context 启发式 -> default。",
    "只预填了已用 /context 实测过的两个。其余请自己确认后再加 ——",
    "分母填错不会报错，只会让占用率系统性偏高或偏低，比不填更危险。",
    "没匹配到的模型会走 default，并在状态行的分母后面显示 ? 提醒你。",
    "确认窗口大小：在该模型下敲 /context，标题行会写 xxx/yyy tokens。"
  ],
  "default": 200000,
  "models": {
    "claude-opus-5[1m]": 1000000,
    "glm-5.2": 200000
  }
}
